import 'package:flutter/material.dart';

class AppColors {
  // Pixel-perfect gradient colors (top -> bottom)
  static const Color gradientTop = Color(0xFF9FD9FF);
  static const Color gradientBottom = Color(0xFFBCE4FF);

  // Cards, text, accents
  static final Color cardBackground = Colors.white.withOpacity(0.95);
  static final Color translucentWhite = Colors.white.withOpacity(0.22);
  static final Color primaryText = Colors.white;
  static final Color subtitleText = Colors.white70;
  static final Color accent = Color(0xFFFFA726); // orange
}

